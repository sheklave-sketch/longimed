"""
Search — stub.
Full implementation: Step 10 of the build order.
"""
from telegram.ext import ConversationHandler

search_conv_handler = ConversationHandler(
    entry_points=[],
    states={},
    fallbacks=[],
    name="search_conv",
)
